/** @type {import('eslint').Linter.Config} */
module.exports = {
  root: true,
  parser: '@typescript-eslint/parser',
  plugins: ['@typescript-eslint'],
  extends: [
    'eslint:recommended',
    'plugin:@typescript-eslint/recommended',
    'prettier',
  ],
  env: {
    node: true,
    es2020: true,
  },
  rules: {
    // A leading underscore marks an argument that is deliberately unused —
    // usually one kept for a signature's shape rather than for its value.
    '@typescript-eslint/no-unused-vars': ['error', { argsIgnorePattern: '^_' }],
  },
  ignorePatterns: [
    '**/dist/**',
    '**/node_modules/**',
    '**/*.cjs',
    // Generated by Next.js in examples/next-app-router, and gitignored there.
    // It is a triple-slash reference file, which this config's rules reject.
    '**/.next/**',
    '**/next-env.d.ts',
  ],
}
