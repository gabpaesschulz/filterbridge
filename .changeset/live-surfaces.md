---
---

Docs, example and tooling for the `onChange` fix — the `queueMicrotask` workaround
removed from the Next.js guide and from `examples/next-app-router`, a re-runnable
`pnpm verify:next-example` script, and release-checklist entries for it and for the
GitHub Release step.

No published package changes.
