---
---

Documentation only — no package code changed, so nothing to release.

This changeset is empty on purpose. `apps/demo/README.md` was edited, and `@filterbridge/demo` is a
workspace package, so `changeset status` asks for one even though no publishable package changed.
