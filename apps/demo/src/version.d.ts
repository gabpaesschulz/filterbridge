/**
 * Injected by Vite's `define` from `packages/core/package.json` — see
 * `apps/demo/vite.config.ts`. The badge in the header is not maintained by
 * hand, so `changeset version` keeps the deployed demo honest on its own.
 */
declare const __FILTERBRIDGE_VERSION__: string
